import * as React from "react";
import styles from "./privateModal.module.css";

function privateModal(props) {
  function closeModal() {
    props.closeModal();
  }

  return (
    <div>
      <div></div>
    </div>
  );
}
