import styles from'./login.module.css';
import styled from 'styled-components';

const bgImgURL = `${process.env.PUBLIC_URL}/images/login.png`


function login() {
  return (
    <div className="login">
        
    </div>
  )
}

export default login